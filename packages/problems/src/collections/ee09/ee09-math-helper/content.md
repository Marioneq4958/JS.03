# Pomocnik Matematyczny (`EE.09-04-19.06`)

Dla fragmentu strony utwórz skrypt, którego wymagania opisano poniżej.

Wymagania dotyczące skryptu (lub skryptów):

- Skrypt jest wykonywany po stronie przeglądarki
- Funkcjonalność może być zaimplementowana w jednym skrypcie lub w wielu
- Zadaniem jest wykonanie działania na liczbach A i B w zależności od wybranego przycisku
- Skrypt uruchamia się po wciśnięciu dowolnego przycisku [...]
- Po uruchomieniu skrypt pobiera wartości z obu pól edycyjnych. Wartości powinny być liczbami całkowitymi. Nie jest wymagane, aby skrypt sprawdzał poprawność danych
- Skrypt wyświetla wynik działania pod przyciskami według wzoru: „Wynik: \<wartość\>”, gdzie
  \<wartość\> oznacza obliczony wcześniej wynik działania

<details class="source-info">
<summary>Źródła i dodatkowe informacje</summary>

> **Źródła**: Wymagania dotyczące skryptu (w formie listy punktowanej) są fragmentem arkusza `EE.09-04-19.06` dostępnego m.in. pod adresem <https://chr1skyy.github.io/Egzamin-Zawodowy-E14-EE09-INF03/ee09/ee09_2019_06_04/ee_09_2019_06_04.pdf>. Szablon, przykładowa odpowiedź oraz testy zostały przygotowane na potrzeby platformy.

> **Wskazówka do testów:** Dla poprawnego działania testów, należy nie usuwać/zmieniać identyfikatorów `a`, `b`, `dodaj`, `odejmij`, `pomnoz`, `podziel`, `potega`, `wynik`. **Wynik działania skryptu umieść w akapicie o id `wynik`.**

</details>
